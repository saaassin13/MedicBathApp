# 代码审查任务列表

## 创建时间
2026-04-27

## 审查范围
整个 Flutter 项目（奶牛药浴移动端）

---

## 任务清单

### 🔴 高优先级

- [ ] 1. 修复 `FarmSelectionNotifier._loadSavedFarm()` 实现不完整
  - 文件: `lib/shared/providers/farm_selection_provider.dart:33-39`
  - 问题: 只设置 `isLoading = false`，未真正恢复 `selectedFarm`
  - 验收标准: 用户重新打开 App 后，奶厅选择状态能正确恢复

### 🟡 中优先级

- [ ] 2. 拆分 `chart_card.dart` 文件（246行过大）
  - 文件: `lib/features/data/presentation/widgets/chart_card.dart`
  - 问题: 混合了绘图逻辑、数据转换、UI 布局
  - 验收标准: 拆分为 `chart_card.dart`、`chart_data_converter.dart`、`chart_painter.dart` 三个文件

- [ ] 3. 删除未使用的方法 `_buildLineSpots()`
  - 文件: `lib/features/data/presentation/widgets/chart_card.dart:161-175`
  - 验收标准: 编译通过，无警告

- [ ] 4. 清理未使用的导入
  - 文件: `lib/features/home/presentation/providers/home_provider.dart`
  - 验收标准: 编译通过，无 unused_imports 警告

- [ ] 5. 实现 TODO 注释中的后端错误上报功能
  - 文件: `lib/services/error/error_reporter.dart:14, 41`
  - 验收标准: 根据后端 API 实际情况实现或标记 `#if PRODUCTION`

- [ ] 6. 提取通用 `DataCard` 组件
  - 问题: `KpiCard`、`StatsCard`、`StatusCard` 存在 50-80% 相似代码
  - 验收标准: 创建基类组件，消除重复代码

### 🟢 低优先级

- [ ] 7. 删除空目录
  - 目录: `lib/features/*/datasources/`、`lib/features/*/repositories/`、`lib/services/mock/`
  - 验收标准: 项目目录中无空目录

- [ ] 8. 统一卡片圆角常量
  - 问题: `AppConstants.cardBorderRadius` 与 `AppTheme` 中硬编码重复
  - 验收标准: 统一使用 `AppConstants.cardBorderRadius`

- [ ] 9. 图表颜色配置分离
  - 问题: `chartBlue` 和 `chartDarkBlue` 放在 `app_colors.dart`
  - 验收标准: 移至 `lib/features/data/domain/chart_colors.dart`

---

## 问题汇总

| 严重程度 | 数量 |
|----------|------|
| 🔴 高 | 1 |
| 🟡 中 | 5 |
| 🟢 低 | 3 |

---

## 已验证无问题模块

- ✅ `lib/core/` - 核心层（常量、路由、主题、工具）设计良好
- ✅ `lib/features/auth/` - 认证模块结构清晰
- ✅ `lib/features/settings/` - 设置模块无问题
- ✅ `lib/shared/widgets/` - 共享组件设计良好
