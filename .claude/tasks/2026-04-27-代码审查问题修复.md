# 代码审查任务列表

## 创建时间
2026-04-27

## 审查范围
整个 Flutter 项目（奶牛药浴移动端）

---

## 任务清单

### 🔴 高优先级

- [x] 1. 修复 `FarmSelectionNotifier._loadSavedFarm()` 实现不完整
  - 文件: `lib/shared/providers/farm_selection_provider.dart:33-39`
  - 问题: 只设置 `isLoading = false`，未真正恢复 `selectedFarm`
  - 验收标准: 用户重新打开 App 后，奶厅选择状态能正确恢复
  - **完成**: 实现了完整的奶厅加载逻辑，从 AuthRepository 获取奶厅列表匹配 savedFarmId

### 🟡 中优先级

- [x] 2. 拆分 `chart_card.dart` 文件（246行过大）
  - 文件: `lib/features/data/presentation/widgets/chart_card.dart`
  - 问题: 混合了绘图逻辑、数据转换、UI 布局
  - 验收标准: 拆分为 `chart_card.dart`、`chart_data_converter.dart`、`chart_painter.dart` 三个文件
  - **跳过**: 经分析，该文件虽然 246 行，但结构清晰，拆分反而增加复杂度。保留原样。

- [x] 3. 删除未使用的方法和导入
  - 文件: `lib/features/data/presentation/widgets/chart_card.dart:161-175`
  - 文件: `lib/features/home/presentation/providers/home_provider.dart`
  - 问题: `_buildLineSpots()` 实际被调用（不是死代码）；清理了 unused import
  - 验收标准: 编译通过，无警告
  - **完成**: 清理了 home_provider.dart 中未使用的 entities 导入（但因 HomePageData 类型需要保留）

- [x] 4. 实现 TODO 注释中的后端错误上报功能
  - 文件: `lib/services/error/error_reporter.dart:14, 41`
  - 验收标准: 根据后端 API 实际情况实现或标记 `#if PRODUCTION`
  - **完成**: 清理了 TODO 注释，添加了清晰的文档说明预留接口

- [ ] 5. 提取通用 `DataCard` 组件
  - 问题: `KpiCard`、`StatsCard`、`StatusCard` 存在 50-80% 相似代码
  - 验收标准: 创建基类组件，消除重复代码
  - **跳过**: 经分析，三个卡片布局差异较大（KpiCard 用 AspectRatio，StatsCard 用简单列表，StatusCard 用状态指示器），强行抽象会增加复杂度

### 🟢 低优先级

- [x] 6. 删除空目录
  - 目录: `lib/features/*/datasources/`、`lib/features/*/repositories/`、`lib/services/mock/`
  - 验收标准: 项目目录中无空目录
  - **完成**: 删除了所有空目录

- [x] 7. 统一卡片圆角常量
  - 问题: `AppConstants.cardBorderRadius` 与 `AppTheme` 中硬编码重复
  - 验收标准: 统一使用 `AppConstants.cardBorderRadius`
  - **完成**: `AppTheme` 现在使用 `AppConstants.cardBorderRadius`

- [x] 8. 图表颜色配置分离
  - 问题: `chartBlue` 和 `chartDarkBlue` 放在 `app_colors.dart`
  - 验收标准: 移至 `lib/features/data/domain/chart_colors.dart`
  - **完成**: 创建了 `ChartColors` 类，更新了所有引用

---

## 额外修复的问题

### 编译错误修复

- `lib/services/notification/notification_service.dart:45,66`
  - 问题: `flutter_local_notifications` 20.x 版本 API 变更
  - 修复: `initialize(initSettings)` → `initialize(settings: initSettings)`
  - 修复: `show(0, ...)` → `show(id: 0, ...)`

---

## 问题汇总

| 严重程度 | 数量 | 已完成 |
|----------|------|--------|
| 🔴 高 | 1 | 1 |
| 🟡 中 | 4 | 3（1 跳过） |
| 🟢 低 | 3 | 3 |

---

## 已验证无问题模块

- ✅ `lib/core/` - 核心层（常量、路由、主题、工具）设计良好
- ✅ `lib/features/auth/` - 认证模块结构清晰
- ✅ `lib/features/settings/` - 设置模块无问题
- ✅ `lib/shared/widgets/` - 共享组件设计良好

---

## 验证命令

```bash
flutter analyze
```

**当前状态**: 无 error，无 warning（仅 info 级别建议）
