# Task 15: 维保记录模块 - 全层实现

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 创建维保记录页面（时间戳精确到毫秒、显示完成状态）

**Architecture:** 维保记录模块 domain/data/presentation 三层

**Tech Stack:** Riverpod, MaintenanceRecordPage

---

## Files

- Create: `lib/features/maintenance_record/domain/entities.dart`
- Create: `lib/features/maintenance_record/domain/maintenance_record_repository.dart`
- Create: `lib/features/maintenance_record/data/mock_maintenance_record_repository.dart`
- Create: `lib/features/maintenance_record/presentation/pages/maintenance_record_page.dart`
- Create: `lib/features/maintenance_record/presentation/providers/maintenance_record_provider.dart`

---

## Step by Step

- [x] **Step 1: 创建 entities.dart**

```dart
/// 维保记录项
class MaintenanceRecordItem {
  final String timestamp; // MM/dd HH:mm:ss.SSS
  final String status; // 完成
  final String taskName;
  final String taskType; // 维保任务

  const MaintenanceRecordItem({
    required this.timestamp,
    required this.status,
    required this.taskName,
    required this.taskType,
  });
}
```

- [x] **Step 2: 创建 maintenance_record_repository.dart**

```dart
import '../maintenance_record_item.dart';

abstract class MaintenanceRecordRepository {
  Future<List<MaintenanceRecordItem>> getMaintenanceRecords();
}
```

- [x] **Step 3: 创建 mock_maintenance_record_repository.dart**

```dart
import '../../domain/maintenance_record_repository.dart';
import '../../domain/maintenance_record_item.dart';

class MockMaintenanceRecordRepository implements MaintenanceRecordRepository {
  @override
  Future<List<MaintenanceRecordItem>> getMaintenanceRecords() async {
    await Future.delayed(const Duration(milliseconds: 500));
    return const [
      MaintenanceRecordItem(
        timestamp: '03/07 11:49:55.099',
        status: '完成',
        taskName: '更换机器人编码器电池',
        taskType: '维保任务',
      ),
      MaintenanceRecordItem(
        timestamp: '03/07 11:49:55.099',
        status: '完成',
        taskName: '喷枪固定螺栓紧固',
        taskType: '维保任务',
      ),
      MaintenanceRecordItem(
        timestamp: '03/07 11:49:55.099',
        status: '完成',
        taskName: '清洗喷头',
        taskType: '维保任务',
      ),
      MaintenanceRecordItem(
        timestamp: '03/07 11:49:55.099',
        status: '完成',
        taskName: '更换喷头',
        taskType: '维保任务',
      ),
    ];
  }
}
```

- [x] **Step 4: 创建 maintenance_record_provider.dart**

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../domain/maintenance_record_repository.dart';
import '../../domain/maintenance_record_item.dart';
import '../../data/mock_maintenance_record_repository.dart';

final maintenanceRecordRepositoryProvider = Provider<MaintenanceRecordRepository>((ref) {
  return MockMaintenanceRecordRepository();
});

final maintenanceRecordsProvider = FutureProvider<List<MaintenanceRecordItem>>((ref) async {
  final repository = ref.watch(maintenanceRecordRepositoryProvider);
  return repository.getMaintenanceRecords();
});
```

- [x] **Step 5: 创建 maintenance_record_page.dart**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import '../../../../core/theme/app_colors.dart';
import '../providers/maintenance_record_provider.dart';

class MaintenanceRecordPage extends ConsumerWidget {
  const MaintenanceRecordPage({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final recordsAsync = ref.watch(maintenanceRecordsProvider);

    return Scaffold(
      backgroundColor: AppColors.backgroundGray,
      appBar: AppBar(
        leading: IconButton(
          icon: const Icon(Icons.close),
          onPressed: () => context.pop(),
        ),
        title: const Text('双城牧场-一期奶厅-一号转盘'),
        actions: [
          IconButton(
            icon: const Icon(Icons.arrow_drop_down),
            onPressed: () {},
          ),
        ],
      ),
      body: recordsAsync.when(
        loading: () => const Center(child: CircularProgressIndicator()),
        error: (error, stack) => Center(child: Text('加载失败: $error')),
        data: (records) => ListView(
          children: [
            Card(
              margin: const EdgeInsets.all(12),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  const Padding(
                    padding: EdgeInsets.all(12),
                    child: Row(
                      children: [
                        Icon(Icons.build, color: AppColors.primaryOrange),
                        SizedBox(width: 8),
                        Text(
                          '维保记录查询',
                          style: TextStyle(fontWeight: FontWeight.bold),
                        ),
                      ],
                    ),
                  ),
                  const Divider(height: 1),
                  ...records.map((record) => Padding(
                        padding: const EdgeInsets.symmetric(
                          horizontal: 12,
                          vertical: 8,
                        ),
                        child: Row(
                          children: [
                            Text(
                              record.timestamp,
                              style: const TextStyle(
                                color: AppColors.textSecondary,
                                fontSize: 12,
                              ),
                            ),
                            const SizedBox(width: 8),
                            Text(
                              record.status,
                              style: const TextStyle(
                                color: AppColors.textPrimary,
                              ),
                            ),
                            const SizedBox(width: 8),
                            Expanded(
                              child: Text(
                                record.taskName,
                                style: const TextStyle(
                                  fontWeight: FontWeight.bold,
                                  color: AppColors.textPrimary,
                                ),
                              ),
                            ),
                            Text(
                              record.taskType,
                              style: const TextStyle(
                                color: AppColors.textSecondary,
                                fontSize: 12,
                              ),
                            ),
                          ],
                        ),
                      )),
                ],
              ),
            ),
            const Center(
              child: Padding(
                padding: EdgeInsets.all(16),
                child: Text(
                  '加载更多...',
                  style: TextStyle(color: AppColors.textSecondary),
                ),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

---

## Verification

- [ ] 维保记录页面显示时间戳（精确到毫秒）、完成状态、任务名称、维保任务
- [ ] 时间格式为 MM/dd HH:mm:ss.SSS
- [ ] 无底部导航栏，关闭按钮返回上一页