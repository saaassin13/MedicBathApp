# Task 12: 记录页面模块 - 全层实现

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 创建记录页面（按日期分组显示药浴记录）

**Architecture:** 记录模块 domain/data/presentation 三层

**Tech Stack:** Riverpod, RecordsPage, Date Grouping

---

## Files

- Create: `lib/features/records/domain/entities.dart`
- Create: `lib/features/records/domain/records_repository.dart`
- Create: `lib/features/records/data/mock_records_repository.dart`
- Create: `lib/features/records/presentation/pages/records_page.dart`
- Create: `lib/features/records/presentation/providers/records_provider.dart`

---

## Step by Step

- [x] **Step 1: 创建 entities.dart**

```dart
/// 记录项
class RecordItem {
  final String startTime;
  final String endTime;
  final int durationSeconds;
  final int cowCount;
  final double recognitionRate;
  final double avgUsage;

  const RecordItem({
    required this.startTime,
    required this.endTime,
    required this.durationSeconds,
    required this.cowCount,
    required this.recognitionRate,
    required this.avgUsage,
  });

  /// 格式化运行时长 XX小时XX分钟XX秒
  String get formattedDuration {
    final hours = durationSeconds ~/ 3600;
    final minutes = (durationSeconds % 3600) ~/ 60;
    final seconds = durationSeconds % 60;
    return '${hours.toString().padLeft(2, '0')}小时'
        '${minutes.toString().padLeft(2, '0')}分钟'
        '${seconds.toString().padLeft(2, '0')}秒';
  }
}

/// 日期分组记录
class DateGroup {
  final String dateLabel; // 今天、昨天、4月5日
  final List<RecordItem> items;

  const DateGroup({
    required this.dateLabel,
    required this.items,
  });
}
```

- [x] **Step 2: 创建 records_repository.dart**

```dart
import '../entities.dart';

abstract class RecordsRepository {
  Future<List<DateGroup>> getRecords();
}
```

- [x] **Step 3: 创建 mock_records_repository.dart**

```dart
import '../../domain/records_repository.dart';
import '../../domain/date_group.dart';
import '../../domain/record_item.dart';

class MockRecordsRepository implements RecordsRepository {
  @override
  Future<List<DateGroup>> getRecords() async {
    await Future.delayed(const Duration(milliseconds: 500));
    return const [
      DateGroup(
        dateLabel: '今天',
        items: [
          RecordItem(
            startTime: '13:11:22',
            endTime: '15:32:01',
            durationSeconds: 8400, // 02小时20分钟00秒
            cowCount: 425,
            recognitionRate: 99.8,
            avgUsage: 4.8,
          ),
          RecordItem(
            startTime: '10:05:00',
            endTime: '12:30:00',
            durationSeconds: 9000, // 02小时30分钟00秒
            cowCount: 380,
            recognitionRate: 99.5,
            avgUsage: 4.6,
          ),
        ],
      ),
      DateGroup(
        dateLabel: '昨天',
        items: [
          RecordItem(
            startTime: '08:00:00',
            endTime: '10:30:00',
            durationSeconds: 9000,
            cowCount: 410,
            recognitionRate: 99.7,
            avgUsage: 4.7,
          ),
        ],
      ),
    ];
  }
}
```

- [x] **Step 4: 创建 records_provider.dart**

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../domain/records_repository.dart';
import '../../domain/date_group.dart';
import '../../data/mock_records_repository.dart';

final recordsRepositoryProvider = Provider<RecordsRepository>((ref) {
  return MockRecordsRepository();
});

final recordsProvider = FutureProvider<List<DateGroup>>((ref) async {
  final repository = ref.watch(recordsRepositoryProvider);
  return repository.getRecords();
});
```

- [x] **Step 5: 创建 records_page.dart**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../../../core/theme/app_colors.dart';
import '../providers/records_provider.dart';

class RecordsPage extends ConsumerWidget {
  const RecordsPage({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final recordsAsync = ref.watch(recordsProvider);

    return Scaffold(
      backgroundColor: AppColors.backgroundGray,
      appBar: AppBar(
        title: const Text('双城牧场-一期奶厅-一号转盘'),
        actions: [
          IconButton(
            icon: const Icon(Icons.arrow_drop_down),
            onPressed: () {},
          ),
        ],
      ),
      body: recordsAsync.when(
        loading: () => const Center(child: CircularProgressIndicator()),
        error: (error, stack) => Center(child: Text('加载失败: $error')),
        data: (dateGroups) => ListView.builder(
          padding: const EdgeInsets.all(12),
          itemCount: dateGroups.length,
          itemBuilder: (context, index) {
            final group = dateGroups[index];
            return Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Padding(
                  padding: const EdgeInsets.symmetric(vertical: 8),
                  child: Text(
                    group.dateLabel,
                    style: const TextStyle(
                      color: AppColors.textSecondary,
                      fontSize: 14,
                    ),
                  ),
                ),
                ...group.items.map((item) => Card(
                      margin: const EdgeInsets.only(bottom: 12),
                      child: Padding(
                        padding: const EdgeInsets.all(12),
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: [
                            Row(
                              mainAxisAlignment: MainAxisAlignment.spaceBetween,
                              children: [
                                Text(
                                  '${item.startTime} - ${item.endTime}',
                                  style: const TextStyle(
                                    fontWeight: FontWeight.bold,
                                    color: AppColors.textPrimary,
                                  ),
                                ),
                                Text(
                                  item.formattedDuration,
                                  style: const TextStyle(
                                    color: AppColors.chartBlue,
                                    fontWeight: FontWeight.bold,
                                  ),
                                ),
                              ],
                            ),
                            const SizedBox(height: 8),
                            Row(
                              children: [
                                _buildStatItem('药浴', '${item.cowCount}头'),
                                _buildStatItem('识别率', '${item.recognitionRate}%'),
                                _buildStatItem('均量', '${item.avgUsage}ML'),
                              ],
                            ),
                          ],
                        ),
                      ),
                    )),
              ],
            );
          },
        ),
      ),
    );
  }

  Widget _buildStatItem(String label, String value) {
    return Expanded(
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            label,
            style: const TextStyle(
              color: AppColors.textSecondary,
              fontSize: 12,
            ),
          ),
          Text(
            value,
            style: const TextStyle(
              color: AppColors.primaryOrange,
              fontWeight: FontWeight.bold,
            ),
          ),
        ],
      ),
    );
  }
}
```

---

## Verification

- [ ] 记录页面按日期分组显示（今天、昨天）
- [ ] 每条记录显示时间区间和中文格式运行时长
- [ ] 显示三栏指标：药浴头数、识别率、均量