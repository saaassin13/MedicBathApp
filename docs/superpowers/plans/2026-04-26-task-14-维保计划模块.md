# Task 14: 维保计划模块 - 全层实现

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 创建维保计划页面（按剩余时间排序、无分组）

**Architecture:** 维保计划模块 domain/data/presentation 三层

**Tech Stack:** Riverpod, MaintenancePlanPage, Remaining Time Sorting

---

## Files

- Create: `lib/features/maintenance_plan/domain/entities.dart`
- Create: `lib/features/maintenance_plan/domain/maintenance_plan_repository.dart`
- Create: `lib/features/maintenance_plan/data/mock_maintenance_plan_repository.dart`
- Create: `lib/features/maintenance_plan/presentation/pages/maintenance_plan_page.dart`
- Create: `lib/features/maintenance_plan/presentation/providers/maintenance_plan_provider.dart`

---

## Step by Step

- [x] **Step 1: 创建 entities.dart**

```dart
/// 维保计划项
class MaintenancePlanItem {
  final String name;
  final int cycleHours; // 维保周期（小时）
  final String lastTime; // 最后一次维保 MM/dd HH:mm:ss
  final int remainingDays;
  final int remainingHours;

  const MaintenancePlanItem({
    required this.name,
    required this.cycleHours,
    required this.lastTime,
    required this.remainingDays,
    required this.remainingHours,
  });

  /// 格式化剩余时间（补零到两位数）
  String get formattedRemaining {
    final days = remainingDays.toString().padLeft(2, '0');
    final hours = remainingHours.toString().padLeft(2, '0');
    return '剩余:${days}天${hours}小时';
  }
}
```

- [x] **Step 2: 创建 maintenance_plan_repository.dart**

```dart
import '../maintenance_plan_item.dart';

abstract class MaintenancePlanRepository {
  Future<List<MaintenancePlanItem>> getMaintenancePlans();
}
```

- [x] **Step 3: 创建 mock_maintenance_plan_repository.dart**

```dart
import '../../domain/maintenance_plan_repository.dart';
import '../../domain/maintenance_plan_item.dart';

class MockMaintenancePlanRepository implements MaintenancePlanRepository {
  @override
  Future<List<MaintenancePlanItem>> getMaintenancePlans() async {
    await Future.delayed(const Duration(milliseconds: 500));
    return const [
      MaintenancePlanItem(
        name: '喷枪固定螺栓紧固',
        cycleHours: 168,
        lastTime: '04/07 00:48:22',
        remainingDays: 6,
        remainingHours: 22,
      ),
      MaintenancePlanItem(
        name: '清洗喷头',
        cycleHours: 168,
        lastTime: '04/07 00:48:22',
        remainingDays: 6,
        remainingHours: 22,
      ),
      MaintenancePlanItem(
        name: '更换喷头',
        cycleHours: 720,
        lastTime: '04/07 00:48:22',
        remainingDays: 12,
        remainingHours: 22,
      ),
      MaintenancePlanItem(
        name: '更换跟随编码器',
        cycleHours: 4320,
        lastTime: '04/07 00:48:22',
        remainingDays: 162,
        remainingHours: 17,
      ),
      MaintenancePlanItem(
        name: '更换机器人润滑脂',
        cycleHours: 11520,
        lastTime: '04/07 00:48:22',
        remainingDays: 462,
        remainingHours: 17,
      ),
      MaintenancePlanItem(
        name: '更换机器人同步带',
        cycleHours: 11520,
        lastTime: '04/07 00:48:22',
        remainingDays: 462,
        remainingHours: 17,
      ),
      MaintenancePlanItem(
        name: '更换机器人编码器电池',
        cycleHours: 17520,
        lastTime: '04/07 00:48:22',
        remainingDays: 712,
        remainingHours: 17,
      ),
      MaintenancePlanItem(
        name: '更换机器人防护服',
        cycleHours: 8760,
        lastTime: '04/07 00:48:22',
        remainingDays: 347,
        remainingHours: 17,
      ),
    ];
  }
}
```

- [x] **Step 4: 创建 maintenance_plan_provider.dart**

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../domain/maintenance_plan_repository.dart';
import '../../domain/maintenance_plan_item.dart';
import '../../data/mock_maintenance_plan_repository.dart';

final maintenancePlanRepositoryProvider = Provider<MaintenancePlanRepository>((ref) {
  return MockMaintenancePlanRepository();
});

final maintenancePlansProvider = FutureProvider<List<MaintenancePlanItem>>((ref) async {
  final repository = ref.watch(maintenancePlanRepositoryProvider);
  return repository.getMaintenancePlans();
});
```

- [x] **Step 5: 创建 maintenance_plan_page.dart**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import '../../../../core/theme/app_colors.dart';
import '../providers/maintenance_plan_provider.dart';

class MaintenancePlanPage extends ConsumerWidget {
  const MaintenancePlanPage({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final plansAsync = ref.watch(maintenancePlansProvider);

    return Scaffold(
      backgroundColor: AppColors.backgroundGray,
      appBar: AppBar(
        leading: IconButton(
          icon: const Icon(Icons.close),
          onPressed: () => context.pop(),
        ),
        title: const Text('双城牧场-一期奶厅-一号转盘'),
        actions: [
          IconButton(
            icon: const Icon(Icons.arrow_drop_down),
            onPressed: () {},
          ),
        ],
      ),
      body: Column(
        children: [
          const Padding(
            padding: EdgeInsets.all(16),
            child: Text(
              '维保计划',
              style: TextStyle(
                color: AppColors.textSecondary,
                fontSize: 14,
              ),
            ),
          ),
          Expanded(
            child: plansAsync.when(
              loading: () => const Center(child: CircularProgressIndicator()),
              error: (error, stack) => Center(child: Text('加载失败: $error')),
              data: (plans) => ListView.builder(
                padding: const EdgeInsets.symmetric(horizontal: 12),
                itemCount: plans.length,
                itemBuilder: (context, index) {
                  final plan = plans[index];
                  return Card(
                    margin: const EdgeInsets.only(bottom: 12),
                    child: Padding(
                      padding: const EdgeInsets.all(12),
                      child: Column(
                        crossAxisAlignment: CrossAxisAlignment.start,
                        children: [
                          Row(
                            mainAxisAlignment: MainAxisAlignment.spaceBetween,
                            children: [
                              Text(
                                plan.name,
                                style: const TextStyle(
                                  fontWeight: FontWeight.bold,
                                  color: AppColors.textPrimary,
                                ),
                              ),
                              Text(
                                '维保:${plan.cycleHours}h',
                                style: const TextStyle(
                                  color: AppColors.textSecondary,
                                  fontSize: 12,
                                ),
                              ),
                            ],
                          ),
                          const SizedBox(height: 4),
                          Text(
                            '最后:${plan.lastTime}',
                            style: const TextStyle(
                              color: AppColors.textSecondary,
                              fontSize: 12,
                            ),
                          ),
                          const SizedBox(height: 4),
                          Align(
                            alignment: Alignment.centerRight,
                            child: Text(
                              plan.formattedRemaining,
                              style: const TextStyle(
                                color: AppColors.primaryOrange,
                                fontWeight: FontWeight.bold,
                              ),
                            ),
                          ),
                        ],
                      ),
                    ),
                  );
                },
              ),
            ),
          ),
        ],
      ),
    );
  }
}
```

---

## Verification

- [ ] 维保计划页面无分组标题，全部显示待执行项
- [ ] 按剩余时间升序排列
- [ ] 卡片显示：项目名称、维保周期、最后一次维保时间、剩余时间（橙色）