# Task 4: 核心层 - 常量与工具

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 配置项目中使用的常量（尺寸、间距）和工具类（日期格式化）

**Architecture:** 核心层 constants + utils 模块

**Tech Stack:** AppConstants, AppDateUtils

---

## Files

- Create: `lib/core/constants/app_constants.dart`
- Create: `lib/core/utils/date_utils.dart`

---

## Step by Step

- [x] **Step 1: 创建 app_constants.dart**

```dart
class AppConstants {
  AppConstants._();

  // 卡片圆角
  static const double cardBorderRadius = 8.0;

  // 内边距
  static const double cardPadding = 12.0;

  // 卡片间距
  static const double cardSpacing = 12.0;

  // 标题栏高度
  static const double appBarHeight = 56.0;

  // 底部导航栏高度
  static const double bottomNavHeight = 56.0;

  // 状态标签尺寸
  static const double statusDotSize = 8.0;

  // KPI 卡片最小高度
  static const double kpiCardMinHeight = 80.0;
}
```

- [x] **Step 2: 创建 date_utils.dart**

```dart
import 'package:intl/intl.dart';

class AppDateUtils {
  AppDateUtils._();

  static final DateFormat _timestampFormat = DateFormat('MM/dd HH:mm:ss.SSS');
  static final DateFormat _timeFormat = DateFormat('HH:mm:ss');
  static final DateFormat _dateFormat = DateFormat('yyyy/MM/dd');

  /// 格式化为时间戳 MM/dd HH:mm:ss.SSS
  static String formatTimestamp(DateTime dateTime) {
    return _timestampFormat.format(dateTime);
  }

  /// 格式化为时间 HH:mm:ss
  static String formatTime(DateTime dateTime) {
    return _timeFormat.format(dateTime);
  }

  /// 格式化为日期 yyyy/MM/dd
  static String formatDate(DateTime dateTime) {
    return _dateFormat.format(dateTime);
  }

  /// 格式化为中文时长 XX小时XX分钟XX秒
  static String formatDuration(int totalSeconds) {
    final hours = totalSeconds ~/ 3600;
    final minutes = (totalSeconds % 3600) ~/ 60;
    final seconds = totalSeconds % 60;
    return '${hours.toString().padLeft(2, '0')}小时'
        '${minutes.toString().padLeft(2, '0')}分钟'
        '${seconds.toString().padLeft(2, '0')}秒';
  }
}
```

---

## Verification

- [x] AppConstants 包含所有项目常量
- [x] AppDateUtils 可正确格式化时间戳、时长